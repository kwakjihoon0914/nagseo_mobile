import { View, Text } from 'react-native';
import Container from '../common/Container';

const Feed = () => {
  return (
    <Container>
      <Text style={{ height: 300 }}>feed</Text>
    </Container>
  );
};

export default Feed;
